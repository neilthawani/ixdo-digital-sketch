import Component from '@ember/component';

export default Component.extend({
    classNames: ["even-column", "project-feedback-container"],

    isInstructor: null,
    feedbackTextAreaFieldWidth: null,
});
